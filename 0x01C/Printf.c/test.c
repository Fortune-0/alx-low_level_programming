/**
 * this is a test file
 * for the printf function in c
 * 
 * it is supposed to handle format specifiers
 * %, %d, %s
 */
#include <stdio.h>

int main()
{
    int a = 4;
    int b = 8;
    printf("i am fortune\n");


        printf("The result of a + b is %d \n", add (a , b));
    return 0;
    

    int add(int a, int b)
    {
    return (a + b);
    }
    


    //printf(" a = %d + b = %d = %d\n", a + b );
    //int a, int b,
    //return (a + b);

}