#include <stdio.h>

int main()
{
    FILE *ptr;
    //ptr = fopen("sample.txt", "r");
    ptr = fopen("sample.txt", "r");

    char = ch;
    fscanf(ptr, "%c", &ch);
    printf("The charater ch is %c", ch);
    
    return 0;
}